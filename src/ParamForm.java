import Library.Schedule;
import javafx.scene.layout.VBox;

import java.lang.reflect.Field;
import java.util.ArrayList;
import java.util.List;

/**
 * Created by josh on 5/30/17.
 * Empty field form, parent
 *
 *
 * Some rules on editability.
 *
 * Teachers are edited by the class;
 *
 * Classes are edited by the student;
 *
 * Build sequential popups until you get what you want.
 *
 * Refresh the form on return focus;
 *
 *
 *
 */
/*public class ParamForm<T extends Schedule> extends VBox {
    private List<Field> primativeFields;


    // This will either be a roster object or a schedule object... hmm.
    private Field collectionField; // These will have a pop of of their own

    private Class CLASS = ((T) new Object()).getClass();

    /**
     * Will create empty field form, e.g. to instantiate new object;
     *//*
    public ParamForm() {
        buildFields();
    }

    public void buildFields() {
        Field[] rawFields = CLASS.getDeclaredFields();
        for (Field f : rawFields) {
            if ()
        }
    }



}*/
